1. Run `git clone https://aur.archlinux.org/dwl.git` in your manual packages folder
2. Run `cd dwl` to move into the folder
3. Replace both of the cloned files with the files in this directory
4. Make sure both `alacritty` and `wmenu` are installed
5. Run `makepkg -sri`
